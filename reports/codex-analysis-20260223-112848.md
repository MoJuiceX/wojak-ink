# Codex Analysis (Night Shift ROI Plan)

Generated: $(date -u +"%Y-%m-%dT%H:%M:%SZ")
Branch: codex/nightly/2026-02-23-nightshift-continue
Worktree: /Users/abit_hex/wojak-ink-nightshift
Window remaining: ~7.5h (operator target)

## Current State Snapshot

### Hard constraints status
- Tests: passing locally (`133` test files / `3984` tests)
- Security: `npm audit --omit=dev` shows `0` known vulnerabilities
- Lint: scoped lint (`src/functions/workers/tests/scripts`) shows `0` errors, `0` warnings
- Bundle budgets (tracked groups): pass (`hard=0`, `soft=0`)

### Important caveat: hidden bundle risk vs shipping target
- The new bundle budget script currently tracks grouped chunks (`entry-index`, `vendor-react`, `vendor-wallet`, etc.) and all pass.
- A standalone static-page bundle exists outside those explicit groups: `dist/assets/wallet-connect-standalone.js` at ~`609.35kB` (raw) from `src/wallet-connect-standalone.ts`.
- This exceeds the project hard constraint (bundle <= `406kB`) if that constraint applies to *all shipped JS assets*, not just main SPA chunks.
- Therefore the repo looks green while still failing the shipping target in practice.

### Tech debt hotspots (current)
- `workers/` still has `0` tests (coverage gap on serverless/async background code).
- Playwright is present but not in CI (CI validates typecheck/lint/unit/build only).
- Night Shift is effective, but there is still process risk from concurrent manual edits vs running supervisor if both use same worktree.
- Existing test suite emits lots of expected stderr noise (not failing, but can hide regressions in logs/reports).

### CI fragility observations
- CI is healthy and now uploads bundle budget artifacts.
- CI still lacks:
  - e2e smoke checks in a non-prod target (by design, safe default)
  - explicit flaky-test detection / retry labeling
  - coverage threshold reporting
- Build step passes but transitive Rollup PURE-comment warning (`ox`) remains noisy/non-actionable.

## Top 5 Improvements by ROI (Impact vs Effort)

### 1. Reduce `wallet-connect-standalone.js` below 406kB (highest priority)
- ROI: **Very high**
- Why:
  - Directly tied to shipping hard constraint (`<=406kB`)
  - Largest current shipped JS asset (~609kB raw)
  - Impacts static flow (`public/free-mints.html`) used outside main SPA bundle budgets
- Likely approaches:
  - Remove/replace `@walletconnect/modal` for static page flow (lighter QR/UI approach)
  - Split modal/protocol/runtime imports or lazy-init deeper for standalone page
  - Externalize/minify aggressively for this one entry (if acceptable for deploy/runtime)
  - Reduce supported WalletConnect methods on static page if library tree-shaking improves
- Effort: Medium
- Risk: Medium (wallet/static page flow)
- Done criteria:
  - `dist/assets/wallet-connect-standalone*.js` <= `406kB` raw (or agreed metric)
  - Static free-mints wallet flow smoke-tested

### 2. Extend bundle budget enforcement to include standalone assets (esp. wallet-connect-standalone)
- ROI: **High**
- Why:
  - Prevents false-green CI when ungrouped bundles regress
  - We already added CI + Night Shift bundle reports; extending coverage is low friction
- Effort: Low
- Risk: Low
- Done criteria:
  - `scripts/bundle-budget-report.mjs` includes explicit budget classification for `wallet-connect-standalone`
  - CI fails on hard regressions for that asset too

### 3. Add worker tests for `workers/did-indexer/worker.ts` critical logic (0 current worker tests)
- ROI: **High**
- Why:
  - Serverless background processing has zero test coverage now
  - High regression risk surface (async/event-driven logic)
- Effort: Medium
- Risk: Low/Medium (test harness only, minimal prod code changes)
- Done criteria:
  - First targeted unit tests for parsing / state transitions / error handling paths in worker
  - Included in `npm run test:unit`

### 4. CI hardening: add lightweight non-prod Playwright smoke workflow (manual/opt-in)
- ROI: **Medium-High**
- Why:
  - Catches integration breakages unit tests miss
  - Repo already has Playwright and safety guard against unattended prod runs
- Effort: Medium
- Risk: Low if non-prod/local target is enforced
- Done criteria:
  - Separate optional workflow/job requiring explicit `TEST_BASE_URL`
  - Never defaults to `https://wojak.ink` in CI/unattended mode

### 5. Night Shift operator-safety hardening: isolate supervisor from manual sessions / worktree locking
- ROI: **Medium**
- Why:
  - We had to pause supervisor to avoid concurrent edits during this analysis
  - This can create merge conflicts or half-written diffs during future overnight iterations
- Effort: Low/Medium
- Risk: Low
- Done criteria:
  - Lock file or branch/worktree ownership check in supervisor/runner
  - Refuses to run if interactive changes are detected (or switches to report-only)

## Suggested Execution Order (Next 2-3 Hours)
1. Bundle constraint work: reduce `wallet-connect-standalone.js`
2. Bundle budget script: classify/enforce standalone wallet bundle
3. Validate (`tsc`, scoped lint, unit tests, build, bundle report)
4. Open PR if green (focused bundle/perf patch)
5. Resume Night Shift loop with remaining queue tasks (report-first if no safe fix queued)

## Notes on Current In-Progress Worktree Diff (paused supervisor)
There is an uncommitted diff in this worktree (README/docs/package/vite/sage-wallet files) likely generated by the paused Night Shift loop, including changes that appear to target wallet bundle splitting. Before making new changes, either:
- inspect and validate that diff, then continue from it, or
- commit/stash/reset it intentionally to avoid mixing unrelated work.

Because the highest-ROI item is the standalone wallet bundle, that in-progress diff is a good first candidate to review and either complete or replace.
